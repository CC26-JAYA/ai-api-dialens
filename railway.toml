[build]
builder = "RAILPACK"

[deploy]
startCommand = "uv run python scripts/start_railway.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
